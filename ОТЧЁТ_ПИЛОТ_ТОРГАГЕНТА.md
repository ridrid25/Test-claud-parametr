# Пилот merchant-agent на данных «Финсрез / МП»: три периода, сухой прогон, рекламный кабинет

Дата: 05.09.2026. Продолжение отчёта `ОТЧЁТ_COMMERCE_AGENTS.md`. Здесь — практика:
на основе демо-данных дашборда сгенерированы три отчётных периода, поверх них
собран бэкенд «только чтение» для merchant-agent из anthropics/commerce-agents,
и агент прогнан через его штатный рантайм. Отдельный раздел — что даёт подключение
рекламного кабинета и как им пользоваться в маркетинговых целях.

**Главное ограничение.** В этой среде нет ключа Anthropic API, поэтому модель
не запускалась. Прогнано всё, что не требует модели: настоящий цикл агента
(`MerchantAgent` из `runtime-messages-api`) со штатным тестовым клиентом
репозитория (`FakeClient`), настоящий исполнитель инструментов, гейты и наш
бэкенд. Раздел 4 «Что предложил бы агент» — это разбор по инструкциям
пяти скиллов на реальных выходах инструментов, сделанный мной, а не ответ
модели. Команда для живого запуска с ключом — в разделе 6.

---

## 1. Что лежит в репозитории

```
agent_pilot/
  generate_periods.py   генератор трёх периодов из демо-данных (seed 2026, только stdlib)
  backend.py            MarketplaceCsvBackend — read-only MerchantBackend поверх CSV
  run_agent.py          --dump | --dry-run | --ask "…" | --digest
  README.md             как запустить
  data/
    sales_p1..p3.csv    отчёты о реализации в формате дашборда (недели, 24 SKU + служебные строки)
    costs.csv           себестоимость по SKU (OZ-006 нарочно без неё)
    stocks_p1..p3.csv   остатки на конец периода
    wb_ads_p1..p3.csv   выгрузка «Статистика кампаний» WB (кампания, артикул, показы, клики, CTR, CPC, затраты, заказы, сумма заказов, CR)
    ozon_ads_p1..p3.csv выгрузка кампаний Ozon (трафареты, продвижение в поиске: показы, клики, расход, заказы, выручка, ДРР)
    scenario.json       что заложено в данные и контрольные итоги
```

Файлы `sales_p*.csv` грузятся в дашборд v36 как есть (проверено headless: 3 файла,
303 строки, ошибок 0, одно подтверждение сопоставления колонок).

---

## 2. Данные: три периода и заложенный сценарий

Периоды по 4 недели: **П1** 01.06–28.06, **П2** 29.06–26.07, **П3** 27.07–23.08.2026.
Основа — 24 товара демо-выгрузки (те же категории, бренды, цены, структура
удержаний), сезон П2 +8 %, П3 −5 %, шум ±15 %.

| | П1 | П2 | П3 |
|---|---|---|---|
| Реализация | 7 385 785 ₽ | 8 058 208 ₽ | 6 523 499 ₽ |
| Возвраты | 460 351 ₽ | 733 716 ₽ | 748 179 ₽ |
| Продвижение | 162 769 ₽ | 175 880 ₽ | 177 247 ₽ |
| К выплате | 4 959 341 ₽ | 5 159 501 ₽ | 3 941 512 ₽ |
| Себестоимость проданного | 3 506 168 ₽ | 3 848 921 ₽ | 3 171 250 ₽ |
| Прибыль после себестоимости | 1 453 173 ₽ | 1 310 580 ₽ | 770 262 ₽ |
| Налог УСН «Доходы» 6 % | 415 526 ₽ | 439 470 ₽ | 346 519 ₽ |
| Заморожено в остатках | 9 330 954 ₽ | 8 818 390 ₽ | 8 565 972 ₽ |
| Цена денег за период (24 %) | 171 792 ₽ | 162 355 ₽ | 157 708 ₽ |

Заложенные события (все видны в данных, см. `scenario.json`):

| Событие | Как проявляется |
|---|---|
| **WB-018 Наушники SonaX 18** — топ-продавец, не пополняется | П2: остаток 107 шт, покрытие 11 дней. П3: продажи по неделям 59 → 33 → 9 → 0 шт, остаток 6 |
| **WB-023 Пледы CozyNest 23** — возвраты растут | доля возвратов от валовой 31 % → 43 % → 50 %; выплата П3 −114 292 ₽, убыток с себестоимостью −365 116 ₽ |
| **OZ-012 Кружки LuceCasa 12** — логистика Ozon дорожает | 12 % → 16 % → 20 % от реализации; маржа после удержаний +2,8 % → −1,6 % → −4,9 % |
| **WB-019 Наушники SonaX 19** — реклама съедает маржу | ДРР 14 % → 16 % → 25 %, в П3 цена −10 %; маржа после удержаний 25 % → 23 % → 9,5 %; ROAS кампании 8,5 → 7,8 → 5,4 |
| **OZ-013 Тарелки Keramo 13** — убыток из-за рекламы + большой сток | маржа −4 % во всех периодах при ДРР 14 %; остаток 1300 → 919 шт, заморожено 663 → 469 тыс. ₽ |
| **WB-002 Худи Aurelka 02** — неликвид | продажи ×0,6, остаток 340 шт без движения, покрытие > 2000 дней |
| **WB-010 Кружки Keramo 10** — аномалия комиссии | недели 2–3 периода П2: комиссия 17 % вместо 15 % |
| Служебные строки | штраф маркировка 620 → 620 → 1860 ₽, эквайринг Ozon 450 ₽/нед |
| Пробел в себестоимости | OZ-006 продаётся, себестоимости нет |

**Сходимость с дашбордом.** П3 через фильтр дат 27.07–23.08 в дашборде v36 и
через бэкенд агента дают одни и те же числа до рубля: к выплате 3 941 512, себестоимость
3 171 250, прибыль 770 262, налог 346 519,20, заморожено 8 565 972, цена денег 157 708 (28 дней).
То есть агент видит ровно то, что видит финансист на экране.

---

## 3. Сухой прогон через штатный рантайм: что подтверждено кодом

Запуск: `python3 -m agent_pilot.run_agent --period 3 --dry-run` (венв с пакетами репозитория).

1. **Инструменты.** Агент предложил модели 23 инструмента: `load_skill`, 8 чтений,
   `get_pending_changes`, 5 `stage_*`, `apply_change`, `discard_change`, память
   (`save_memory`, `recall_memories`), 4 презентационных (`present_metrics`,
   `present_digest`, `present_change_preview`, `present_suggestions`).
2. **Восемь чтений отвечают нашими данными** внутри ограждения `<merchant_data>…</merchant_data>`.
   Пример того, что увидела модель от `get_business_snapshot` за П3:
   ```
   {"period": "2026-07-27/2026-08-23", "compare_to": "2026-06-29/2026-07-26",
    "sales": 5775320.0, "orders": 0, "average_order_value": 1955.8,
    "sales_change_pct": -21.2, "orders_change_pct": -14.6, "currency": "RUB",
    "alerts": {"low_stock": 1, "slow_movers": 2, "order_issues": 1, "pending_changes": 0},
    "note": "Заказов, трафика и конверсии в отчёте о реализации нет; ..."}
   ```
   (`orders` в типе `BusinessSnapshot` — обязательный `int`, `None` туда не положить;
   поэтому 0 с пояснением в `note`. Это ограничение типов репозитория, а не наших данных.)
3. **Лимит вызовов.** `max_tool_iterations = 8` за один ход: девятый вызов инструмента
   обрывается текстом «The turn was interrupted before this call returned». Для
   дайджеста по 24 SKU это значит: агент должен опираться на агрегаты (алерты,
   snapshot, серии), а не ходить по карточкам по одной.
4. **Гейт провенанса срабатывает до бэкенда.** Попытка `stage_price_update` для
   WB-019 без предварительного `search_listings`/`get_listing` в этой же сессии
   заблокирована самим рантаймом: «listing ids WB-019 were not returned by catalog
   tools in this session». То же для `apply_change` по несуществующему `change_id`.
5. **Отказ записи работает как задумано.** После `search_listings` те же `stage_price_update`
   и `stage_inventory_action` дошли до бэкенда и вернули модели наш текст ошибкой инструмента:
   «Этот пилот работает только на чтение: изменения цен, остатков, карточек и кампаний в кабинетах
   Wildberries/Ozon отсюда не делаются. Предложите оператору, что изменить, и он сделает это в
   кабинете сам.» Никакого другого пути к записи у агента нет.

Итог по разделу: связка «наши выгрузки → MerchantBackend → рантайм агента» работает без
правок в коде репозитория. Не проверено: качество текста и выбор инструментов живой моделью.

---

## 4. Что предложил бы агент по периодам

Ниже — разбор по инструкциям скиллов (`performance-insights`, `inventory-operations`,
`pricing-promotions`, `marketing-campaigns`) на фактических выходах инструментов
за каждый период. Это моя имитация хода агента по правилам скиллов, не ответ модели.
Формат — как требует `present_digest`: 3–6 пунктов, ранжированных по деньгам под
риском, затем по срочности.

### П1 (01.06–28.06) — базовый период

Инструменты: snapshot без сравнения (прошлого периода нет), 1 slow mover, 0 issues,
7 кампаний.

1. **Убыточные с учётом себестоимости: 5 SKU, −117 тыс. ₽.** WB-023 Пледы CozyNest 23 (−58 920),
   WB-021 Пледы Aurelka 21 (−23 785), OZ-016 Тарелки Keramo 16 (−17 280), WB-022 (−10 636),
   OZ-013 (−6 644). В них заморожено 2,31 млн ₽ остатков.
2. **OZ-013 продаётся ниже безубыточности.** `get_pricing_context`: цена 791 ₽, себестоимость 510 ₽,
   маржа после удержаний −4,1 %, минимальная цена (точка безубыточности) 846 ₽. Реклама даёт ДРР 14 %
   на убыточный товар. Скилл цен предложит один из двух вариантов на выбор оператора: цена 850 ₽
   (+7,4 %, внутри лимита 20 %) или остановить кампанию OZ-C-202 по этому SKU.
3. **WB-002 неликвид:** 340 шт, покрытие 2380 дней, заморожено 92 тыс. ₽. Три варианта по
   имени, как требует скилл: оставить, уценить (передача скиллу цен), снять.
4. **Кампании:** ROAS по данным кабинета от 4,0 (OZ-C-202, тарелки) до 14,8 (OZ-C-201, кружки LuceCasa).
   Скилл маркетинга обязан назвать эти цифры «заявлением канала» и спросить, какая цель у
   кампаний, прежде чем называть их хорошими или плохими. Цели в памяти нет — агент задаст вопрос.
5. **Пробел данных:** нет себестоимости OZ-006 (395 шт остатка, продаётся) — прибыль по нему завышена.

Что агент **не увидит** в П1: ничего аномального в комиссии (аномалия — в П2).

### П2 (29.06–26.07) — рост продаж, но проблемы созревают

Snapshot: продажи +5,8 %, штуки +7,9 % к П1. Алерты: 1 low stock, 2 slow movers, 1 issue.

1. **WB-023: всплеск возвратов.** `get_order_issues`: «возвраты 43 % от валовой (в прошлом периоде 30 %),
   484 136 ₽ за период». Выплата по SKU упала до 16 тыс. ₽ при реализации 636 тыс. ₽,
   убыток с себестоимостью −280 953 ₽ — хуже всех. Скилл требует цитировать факт и не
   придумывать причину: «причины возвратов в отчёте не указаны» — агент так и скажет и
   предложит оператору проверить карточку/размерную сетку/брак. Это первый пункт дайджеста
   по деньгам.
2. **WB-018 заканчивается.** `get_inventory_alerts`: остаток 107 шт, покрытие 11 дней,
   продано 273 шт за период — это лучший товар (прибыль 457 тыс. ₽ за П2). По скиллу
   inventory-operations агент предложит пополнение с явным количеством и расчётом:
   «≈ 68 шт в неделю; 300 шт покрывают месяц с запасом». В пилоте `stage_inventory_action`
   откажет — агент напишет это оператору текстом.
3. **OZ-012 ушёл в минус из-за логистики.** `query_metrics('logistics', segment='sku:OZ-012')`:
   29 195 → 31 229 → 26 854 → 26 343 ₽ в неделю против 16–19 тыс. в П1; доля 16 % против 12 %.
   Маржа после удержаний −1,6 %, минимальная цена 3 052 ₽ против текущей 2 980 ₽. Спрос
   «rising». Предложение скилла цен: +3 % к цене (до 3 070 ₽) или пересмотр схемы доставки
   (это уже решение оператора вне агента).
4. **Прибыль периода упала при росте продаж:** 1 453 → 1 311 тыс. ₽ (−9,8 %) при продажах +5,8 %.
   Скилл performance-insights: «отделить микс от уровня». Разложение по сегментам даёт
   ответ: Дом −148 тыс. ₽ к выплате (пледы), Ozon-логистика +40 тыс. ₽, остальное выросло.
5. **Комиссия WB-010 17 % вместо 15 % в двух неделях.** Агент **это не найдёт сам**: в интерфейсе
   нет метода «проверка тарифа», а `query_metrics('commission', segment='sku:WB-010')` он вызовет
   только если оператор спросит про комиссию. Дашборд v36 находит это аудитом переплат
   автоматически. Вывод: аудит удержаний остаётся сильной стороной дашборда, агенту его нужно
   отдавать готовым (например, как `order_issues` нового вида или через `get_merchant_context`).

### П3 (27.07–23.08) — падение, которое надо объяснить

Snapshot: продажи −21,2 %, штуки −14,6 % к П2. Прибыль 770 тыс. ₽ против 1 311 тыс. (−41 %).

1. **Почему упала выплата.** Скилл: сначала подтвердить движение (период полный, 4 недели,
   дыр нет), затем разложить. Серия `payout` по неделям: 1 140 → 1 072 → 996 → 944 тыс. ₽ —
   падение внутри периода, не разовый провал. Сегменты к выплате П2 → П3: Электроника
   1 638 → 977 тыс. (−661 тыс.), Дом 423 → 197 тыс. (−226), Одежда 1 008 → 877 (−131),
   Посуда 2 093 → 1 895 (−198). Больше половины падения — электроника, и это один SKU:
2. **WB-018 кончился.** Продажи по неделям 59 → 33 → 9 → 0 шт, остаток 6. В П2 он давал
   68 шт в неделю и 457 тыс. ₽ прибыли за период; в П3 — 167 тыс. Недополученная прибыль
   ≈ 290 тыс. ₽ за период, и дальше будет ноль. Это верхняя строка дайджеста: деньги под
   риском максимальны, окно закрыто. Предложение: срочная поставка; пока товара нет —
   остановить любую рекламу на него (в кампаниях WB его нет, что правильно).
3. **WB-023: 50 % возвратов, выплата отрицательная (−114 тыс. ₽), убыток −365 тыс. ₽.** Второй
   период подряд. Скилл inventory-operations для проблемного товара предложит паузу продаж
   («pause») с обоснованием: каждая проданная единица приносит убыток, остаток 450 шт
   (заморожено 672 тыс. ₽). Решение — оператора.
4. **WB-019: реклама съела маржу.** ДРР 25 %, цена снижена до 797 ₽, маржа после удержаний
   9,5 %, прибыль 17 тыс. ₽ против 43–45 тыс. в П1–П2 при большем количестве проданного.
   Кампания WB-C-101 (SonaX 19+20): расход 65 тыс. ₽, ROAS 5,4 против 8,5 в П1. Скилл
   маркетинга: бюджет фиксирован, значит «снизить ставку на WB-019 в WB-C-101 и вернуть цену
   к 880 ₽» с оговоркой, что атрибуция кабинета — заявление канала.
5. **OZ-012: логистика 20 % от реализации, маржа −4,9 %,** минимальная цена 3 239 ₽ против
   2 999 ₽ (+8 %, внутри лимита). Кампания OZ-C-201 на него — 27 тыс. ₽ с ROAS 12,3 — лучшая
   по ROAS кампания рекламирует убыточный товар. Реклама масштабирует убыток.
6. **Деньги в остатках.** Заморожено 8,57 млн ₽, цена денег 158 тыс. ₽ за 28 дней при 24 %.
   Из них 3,14 млн ₽ — в убыточных SKU (WB-023, OZ-012, WB-021, OZ-016, WB-022, OZ-013).
   Налог УСН 6 % за период 347 тыс. ₽ — агент возьмёт из `get_merchant_context.finance`
   и обязан подписать, что по SKU это распределение, а не факт (там так и написано).

Итого по П3 дайджест, который агент собрал бы из инструментов: WB-018 (пополнить),
WB-023 (пауза), OZ-012 и OZ-013 (цена вверх или стоп реклама), WB-019 (ставка вниз, цена
назад), заморожено 3,1 млн ₽ в убыточных. Все пять — предложения оператору; ни одно не
исполняется агентом.

### Что агент делает хуже дашборда, а что лучше

| Лучше у агента | Лучше у дашборда |
|---|---|
| Ранжирование по деньгам под риском в 5 строк вместо шести вкладок | Сходимость каскада до рубля, аудит комиссии/удержаний, проверка тождеств отчёта |
| Связка фактов между источниками: остаток + темп продаж + прибыль на единицу + реклама | Полная таблица всех SKU с сортировкой и группировкой |
| Расчёт «что если» в диалоге на формуле маржи, которую задаём мы | Экспорт в Excel в форме, принятой заказчиком |
| Память целей и решений между периодами (в пилоте не включена) | Ноль затрат на токены и нулевая задержка |

---

## 5. Рекламный кабинет: что даёт и как использовать в маркетинговых целях

### 5.1. Что есть в выгрузках и что мы из них берём

В пилоте выгрузки кабинетов смоделированы по составу полей, который отдают оба кабинета
в статистике кампаний: кампания (id, название, тип), дата, артикул, показы, клики, CTR,
CPC (WB), затраты/расход, заказы, сумма заказов/выручка, CR (WB), ДРР (Ozon). Реальные
файлы отличаются названиями колонок и разбивкой (по дням, по ключевым фразам) — для
подключения нужен такой же разбор с алиасами колонок, как для отчёта о реализации.

Сумма затрат кампаний по SKU и неделе в пилоте **равна** столбцу «Продвижение» отчёта о
реализации — это контрольное тождество, которое надо проверять и на реальных данных:
если реклама в кабинете больше, чем удержано в отчёте, часть расхода прошла другой
статьёй или другим периодом.

В `MerchantBackend` выгрузка становится `get_campaign_performance`: одна `Campaign` на
кампанию за период, `spend` = расход, `revenue` = выручка по атрибуции кабинета,
`budget` = расход (кабинет бюджета не отдаёт; об этом сказано в `limitations`).

### 5.2. Главная идея: считать рекламу от прибыли, а не от заказов

Кабинет показывает ROAS = выручка заказов / расход. По этой метрике в П3 лучшая кампания —
OZ-C-201 (ROAS 12,3) на кружки LuceCasa OZ-012, у которых маржа после удержаний **−4,9 %**.
Реклама там эффективна по кабинету и убыточна по факту. Только наш дашборд знает маржу
после комиссии, логистики, хранения, себестоимости и налога — поэтому кабинет надо
подключать **к дашборду**, а не читать отдельно.

Правило, которое ложится в бэкенд как поле `PricingContext` или в контекст агента:

> **ДРР-порог SKU** = маржа после удержаний и себестоимости до рекламы, минус целевая
> маржа. Реклама на SKU допустима, пока фактический ДРР ниже порога.

По данным П3:

| Кампания | SKU | Расход | ROAS кабинета | Маржа SKU после удержаний | Вывод |
|---|---|---|---|---|---|
| OZ-C-201 Трафареты | OZ-012 | 27 234 ₽ | 12,3 | −4,9 % | стоп: масштабирует убыток |
| OZ-C-202 Продвижение в поиске | OZ-013, OZ-016 | 37 431 ₽ | 3,7 | −3,4 % / отриц. | стоп: оба SKU убыточны |
| WB-C-101 Аукцион | WB-019, WB-020 | 65 243 ₽ | 5,4 | 9,5 % / положит. | снизить ставку на WB-019, оставить WB-020 |
| WB-C-102 Авто | WB-001, WB-003 | 16 372 ₽ | 13,6 | положит. | масштабировать: остаток 476/153 шт |
| WB-C-103 Авто | WB-010 | 15 704 ₽ | 13,9 | положит. | масштабировать: остаток 546 шт |
| WB-C-104 Аукцион | WB-024 | 8 743 ₽ | 14,6 | положит. | оставить |
| OZ-C-203 Трафареты | OZ-008 | 6 520 ₽ | 14,8 | положит. | оставить |

При фиксированном бюджете 177 тыс. ₽ перенос 65 тыс. ₽ (OZ-C-201 + OZ-C-202) на
WB-C-102/103 — это ровно то предложение, которое скилл `marketing-campaigns` умеет
формулировать («названа кампания, которой добавили, и та, у которой забрали»), с
обязательной оговоркой про атрибуцию.

### 5.3. Четыре правила, которые стоит зашить в контекст агента

1. **Нет маржи — нет рекламы.** SKU с маржой после удержаний ≤ 0 не рекламируется, пока не
   поднята цена или не снижена себестоимость. В П3 это OZ-012, OZ-013, OZ-016.
2. **Нет остатка — нет рекламы.** SKU с покрытием < 14 дней снимается с рекламы: платить за
   клики по товару, которого через неделю не будет, бессмысленно. В П3 это WB-018.
3. **ДРР не выше порога.** Для WB-019 маржа до рекламы ≈ 34 % (9,5 % + ДРР 25 %); при целевой
   марже 20 % порог ДРР 14 %. Фактический 25 % — в 1,8 раза выше.
4. **Смотреть на ROAS только как на «заявление канала».** Пересчитывать в прибыль: прибыль
   на единицу × заказы по атрибуции − расход. Скилл это требует текстом, но цифры
   должны прийти из бэкенда.

### 5.4. Что ещё даст кабинет, кроме бюджета

- **Черновики кампаний и текстов.** Скилл маркетинга пишет текст только из фактов карточки
  (`get_listing`) или слов оператора. В наших данных карточки без описаний и атрибутов, кроме
  бренда, — агент честно спросит, а не придумает. Чтобы черновики были полезны, в бэкенд надо
  отдать описания карточек (выгрузка карточек из кабинета, не рекламного).
- **Сезонные и lifecycle-сценарии** (win-back, «последний шанс») в скилле есть, но на
  маркетплейсе нет своей аудитории и нет канала рассылок — эти части скилла для WB/Ozon пустые.
  Остаётся: аукционные ставки, авто-кампании, трафареты, акции площадки.
- **Акции площадки.** `stage_promotion` (скидка, охват, даты) подходит для участия в акциях WB/Ozon:
  агент посчитает, какие SKU входят в акцию не ниже точки безубыточности (`min_price`), и
  подпишет те, что «под полом». Это уже реализовано в нашем `get_pricing_context`.

### 5.5. Честно про ограничения

- Бюджета и ставок в статистике нет — управлять ставками агент не сможет даже в режиме записи
  без отдельной интеграции с API продвижения площадки. В пилоте это в любом случае отказ.
- Атрибуция кабинета завышена (заказы «после клика» включают органику). В данных П3 для WB-019
  атрибуция снижена до 30 %, чтобы это было видно; на реальных данных проверять сходимостью
  «заказы по кабинету ≤ продано по отчёту».
- Разбивка по ключевым фразам, поисковые позиции, конверсия карточки — этого в отчёте о
  реализации нет, а в кабинете есть. Агенту это можно отдать через `query_metrics` с новыми
  именами метрик, но скиллы про них не знают: нужен свой скилл «реклама маркетплейса».

---

## 6. Как запустить с ключом

```bash
git clone --depth 1 https://github.com/anthropics/commerce-agents /path/commerce-agents
cd /path/commerce-agents && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
cd /path/Test-claud-parametr
export COMMERCE_AGENTS_DIR=/path/commerce-agents
export ANTHROPIC_API_KEY=...            # ключ только на этой машине
/path/commerce-agents/.venv/bin/python -m agent_pilot.run_agent --period 3 --digest
/path/commerce-agents/.venv/bin/python -m agent_pilot.run_agent --period 3 --ask "Почему упала выплата к прошлому периоду и что делать первым?"
/path/commerce-agents/.venv/bin/python -m agent_pilot.run_agent --period 2 --ask "Какая цена на OZ-012 даст 15% после удержаний? Реклама на него оправдана?"
```

Модель по умолчанию `claude-opus-5` с `thinking_effort="low"`, до 8 вызовов инструментов
за ход. Стоимость одного хода репозиторий описывает как «несколько центов». Без ключа:
`--dump` (выходы всех чтений) и `--dry-run` (сценарий через рантайм).

---

## 7. Что подтверждено, что нет

**Подтверждено кодом и прогоном:** бэкенд на CSV реализует интерфейс без правок ядра;
восемь чтений отдают данные, сходящиеся с дашбордом до рубля; гейты провенанса и отказ
записи работают; лимит 8 вызовов за ход; типы `BusinessSnapshot` не позволяют `orders=None`.

**Не подтверждено (нужен ключ):** формулировки живой модели, выбор ею инструментов,
качество дайджеста и черновиков, поведение памяти между периодами, стоимость хода.

**Вывод для продукта.** Пилот стоит запускать в режиме «только чтение» на периодах П2–П3:
там агент находит то, что финансист ищет руками по шести вкладкам, и предлагает конкретные
действия с числами. Рекламный кабинет подключать сразу, но считать рекламу от маржи после
удержаний, а не от ROAS кабинета — иначе агент, как и кабинет, будет хвалить убыточные кампании.

---

## 8. Живой прогон: что модель сказала на самом деле

Дополнение от 05.09.2026, вечер. Пять ходов агента выполнены на компьютере заказчика
(модель `claude-opus-5`, ключ остался на его машине), результаты — в
`agent_pilot/results/*.json`, коммит 8938f69. Ниже сравнение с прогнозом из раздела 4.

### 8.1. Что совпало с прогнозом

| Прогноз (раздел 4) | Живой ответ модели | Итог |
|---|---|---|
| П2: первым пунктом WB-023, всплеск возвратов 43 % против 30 %, 484 136 ₽ | Первый пункт дайджеста П2, те же цифры, «крупнейшая утечка денег; причины возвратов в отчёте не указаны» | совпало |
| П2: WB-018 кончается, 107 шт, покрытие 11 дней, поставку запускать | Второй пункт: «107 шт при пороге 136, покрытие 11 дней … поставку надо запускать сегодня» | совпало |
| П3: WB-018 — верхняя строка по деньгам | В дайджесте П3 второй (после WB-023); в ответе про выплату — «первым делом WB-018 … единственный ходовой SKU, который на следующей неделе гарантированно выпадет из выплаты» | совпало |
| П3: падение выплаты объяснить, подтвердив движение по неделям | «3 941 512 ₽ против 5 159 501 ₽ (−23,6 %), падение идёт непрерывно неделя к неделе»; модель сама запросила серию за два периода (8 недель) и отделила объём от доли выплаты: «почти всё падение объёмное, а не комиссионное; доля выплаты просела 70,4 % → 68,2 %» | совпало и точнее прогноза |
| Неликвид WB-002: три варианта по имени | «решение: оставить, уценить или снять» во всех трёх дайджестах | совпало |
| Реклама: без маржи в данных модель будет ранжировать по ROAS кабинета и хвалить убыточные кампании | Именно так: OZ-C-202 (ROAS 3,7) — «снять 25 000 ₽», доливать в OZ-C-203, WB-C-104, WB-C-102/103 по ROAS; OZ-C-201 с ROAS 12,3 на убыточном OZ-012 не упомянута вовсе | совпало, см. 8.2 |
| Оговорка про атрибуцию | «Выручка здесь — атрибуция кабинета, а не выручка, которой без рекламы точно не было бы»; прогноз прироста подписан «ожидание, а не факт» | совпало |
| Только чтение | В каждом ответе: «изменения вы вносите в кабинете сами» | совпало |

Все числа в ответах модели сходятся с выходами инструментов и с дашбордом: выплата,
выручка, налог 415 526 ₽ со всей выручки 6 925 434 ₽ (П1), заморожено 9 330 954 ₽ и
цена денег 171 792 ₽. Ни одной выдуманной цифры, кроме одного случая ниже.

### 8.2. Что модель пропустила или сделала хуже прогноза

1. **Убыточные SKU не попали ни в один дайджест.** Ни в П1 (5 SKU, −117 тыс. ₽), ни в П3
   (WB-023 −365 тыс., OZ-012 −33 тыс., OZ-016 −18 тыс.). Причина понятна из инструментов:
   в интерфейсе нет метода «убыточные товары», а `get_pricing_context` модель за 8 вызовов
   на 24 SKU не вызывает. Убыток виден только по одному SKU за раз.
2. **OZ-012 и рост логистики Ozon не замечены** ни в П2, ни в П3. Модель не спрашивала
   `query_metrics('logistics', segment='sku:OZ-012')` — некому было подсказать.
3. **Реклама оценена только по ROAS кабинета.** Модель предложила разумное перераспределение
   и сама же в подсказках написала «Посчитать маржу с учётом рекламы по этим SKU», но в
   ответ этого не включила. OZ-C-201 (лучший ROAS, товар с маржой −4,9 %) названа не была;
   WB-C-101 — «держать, не наращивать» без упоминания, что у WB-019 ДРР 25 % при марже 9,5 %.
4. **Одна вычисленная модельно цифра оказалась неверной.** В дайджесте П1: «WB-002 … в
   остатке заморожено около 246 тыс. руб. по цене 724 руб.». Модель умножила остаток на
   цену продажи (340 × 724), а не на себестоимость (340 × 271 = 92 тыс. ₽). Это ровно то,
   от чего скилл цен предостерегает («quote margins, do not compute them»), но про
   замороженные деньги такого правила нет, а готовой цифры по SKU бэкенд не отдавал.
5. **Разложение падения по сегментам не сделано.** В ответе про выплату модель верно
   назвала падение объёмным и указала на WB-018, но категорийную раскладку
   (электроника −661 тыс. ₽ из −1 218 тыс.) не построила, предложив её как следующий
   шаг. Для одного хода с лимитом 8 вызовов это ожидаемо.
6. **Ответ на дайджест П3 пришёл без текста** — только компонент `present_digest` и
   подсказки. Для интерфейса это нормально (карточка есть), для консольного вывода — пусто.
7. В П1 модель записала WB-002 в «проблемные заказы» с возвратами 29,8 %, хотя бэкенд
   порог 30 % не пробил и issue не выдавал. Это её собственный вывод из карточки;
   он не ложный, но подан как алерт.

### 8.3. Что изменено в бэкенде по итогам (коммит после 8938f69)

Принцип: то, что модель должна цитировать, а не вычислять, бэкенд отдаёт готовым.

- `get_campaign_performance`: в `objective` каждой кампании — маржа после удержаний
  каждого её SKU: «OZ-C-201 … OZ-012 маржа после удержаний −4,9 %», «OZ-C-202 … OZ-013 −3,4 %;
  OZ-016 −15,0 %», «WB-C-101 … WB-019 +9,5 %; WB-020 +37,2 %». Теперь ROAS и убыточность
  товара приходят в одном объекте.
- `get_merchant_context`: списки `loss_making_skus` (SKU, прибыль после себестоимости,
  маржа, расход на рекламу, остаток, заморожено) и `frozen_by_sku_top`, плюс явная
  формула `frozen_note`: «заморожено = остаток × себестоимость за шт (не × цена продажи)».
  Это закрывает пункты 1, 3 и 4 из 8.2 без единой правки в ядре репозитория.

Проверено: `--dump` и `--dry-run` на периоде 3 проходят, 8 чтений без ошибок.

Что стоит перепрогнать с ключом, чтобы убедиться: `--period 3 --digest` (должны появиться
убыточные SKU и корректная сумма заморозки) и `--period 3 --ask "Какие рекламные кампании
остановить, какие усилить, при том же бюджете?"` (ожидание: остановить OZ-C-201 и OZ-C-202
как рекламу убыточных товаров, снизить ставку по WB-019).

### 8.4. Вывод после живого прогона

Агент в режиме «только чтение» работает и говорит по делу: приоритизирует по деньгам,
не выдумывает причины, оговаривает атрибуцию, помнит про запрет на запись. Слабое место
не в модели, а в том, что мы ей отдаём: всё, чего нет готовой цифрой в инструментах
(убыток по SKU, маржа рядом с кампанией, заморожено по себестоимости), она либо не
находит, либо считает сама и может ошибиться. Вывод для пилота прежний, но с уточнением:
финансовые агрегаты дашборда (убыточные SKU, аудит удержаний, заморожено) надо
отдавать агенту в контексте готовыми, тогда он их цитирует, а не вычисляет.
